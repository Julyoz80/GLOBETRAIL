<h1>Chats for <%= @travel.country %></h1>

<div class="mb-4">
  <% if @chats.any? %>
    <ul>
      <% @chats.each do |chat| %>
        <li><%= link_to chat.title, travel_chat_path(@travel, chat) %></li>
      <% end %>
    </ul>
  <% else %>
    There are no chats yet.
  <% end %>
</div>

<%= simple_form_for [@travel, @chat] do |f| %>
  <%= f.button :submit, "New chat", class: "btn btn-primary" %>
<% end %>
